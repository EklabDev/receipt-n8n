# Prompt: implementer-python-fastapi

**Skill id:** `implementer-python-fastapi`

FastAPI services with validation and dependency injection.

---

Implementer: Python + FastAPI

## When to use
FastAPI route or service change.

## MUST
- Pydantic models for IO; explicit status codes and errors.
- Dependency overrides for tests; settings via environment.
- Apply security-redlines for secrets and user data.

## Verification
- [ ] OpenAPI generation checked if enabled
